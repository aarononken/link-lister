<h2>User Index</h2>

<table>
<tbody>
<% @users.each do |user| %>
	<tr>
    <td><%= image_tag avatar_url(user) %><td>
    <td><%= link_to user.username, user %></td>
		<td><%= user.email %></td>
	</tr>
<% end %>
</tbody>
</table>