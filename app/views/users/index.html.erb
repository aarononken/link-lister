<h2>User Index</h2>

<table>
<tbody>
<% @users.each do |user| %>
	<tr>
    <td><%= image_tag avatar_url(user) %><td>
		<td><%= user.username %></td>
		<td><%= user.email %></td>
	</tr>
<% end %>
</tbody>
</table>